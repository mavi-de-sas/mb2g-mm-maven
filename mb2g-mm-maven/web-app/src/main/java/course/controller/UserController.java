package course.controller;

import cource.entity.User;
import course.domain.UserCommand;

public class UserController {
    User saveUser(UserCommand userCommand) {
        return new User();
    }
}
